{
  "name": "WF2 — 10:00 AM Admin Report",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [{ "field": "cronExpression", "expression": "30 4 * * 1-6" }]
        }
      },
      "id": "wf2-n01",
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "mode": "manual",
        "assignments": {
          "assignments": [
            { "id": "c1", "name": "SUPABASE_URL", "value": "YOUR_SUPABASE_URL_HERE", "type": "string" },
            { "id": "c2", "name": "SUPABASE_SERVICE_ROLE_KEY", "value": "YOUR_SERVICE_ROLE_KEY_HERE", "type": "string" },
            { "id": "c3", "name": "COMPANY_ID", "value": "YOUR_COMPANY_UUID_HERE", "type": "string" }
          ]
        },
        "options": {}
      },
      "id": "wf2-n02",
      "name": "Config",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [460, 300]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').item.json.SUPABASE_URL }}/rest/v1/users?company_id=eq.{{ $('Config').item.json.COMPANY_ID }}&status=eq.active&select=id,name,employee_id,phone,role",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "apikey", "value": "={{ $('Config').item.json.SUPABASE_SERVICE_ROLE_KEY }}" },
            { "name": "Authorization", "value": "=Bearer {{ $('Config').item.json.SUPABASE_SERVICE_ROLE_KEY }}" }
          ]
        },
        "options": {}
      },
      "id": "wf2-n03",
      "name": "Get Users",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [680, 300]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').item.json.SUPABASE_URL }}/rest/v1/attendance_logs?date=eq.{{ $now.format('yyyy-MM-dd') }}&select=user_id,clock_in",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "apikey", "value": "={{ $('Config').item.json.SUPABASE_SERVICE_ROLE_KEY }}" },
            { "name": "Authorization", "value": "=Bearer {{ $('Config').item.json.SUPABASE_SERVICE_ROLE_KEY }}" }
          ]
        },
        "options": {}
      },
      "id": "wf2-n04",
      "name": "Get Today Logs",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [900, 300]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $('Config').item.json.SUPABASE_URL }}/rest/v1/attendance_logs?date=gte.{{ $now.format('yyyy-MM') }}-01&date=lte.{{ $now.format('yyyy-MM-dd') }}&select=user_id,clock_in",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "apikey", "value": "={{ $('Config').item.json.SUPABASE_SERVICE_ROLE_KEY }}" },
            { "name": "Authorization", "value": "=Bearer {{ $('Config').item.json.SUPABASE_SERVICE_ROLE_KEY }}" }
          ]
        },
        "options": {}
      },
      "id": "wf2-n05",
      "name": "Get Month Logs",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1120, 300]
    },
    {
      "parameters": {
        "jsCode": "const LATE_CUTOFF   = '09:30'\nconst MAX_PASSES    = 5\nconst IST_OFFSET_MS = 5.5 * 60 * 60 * 1000\n\nconst users     = $('Get Users').first().json\nconst todayLogs = $('Get Today Logs').first().json\nconst monthLogs = $('Get Month Logs').first().json\n\nconst adminUser  = users.find(u => u.role === 'ADMIN')\nconst adminPhone = adminUser?.phone\n\nconst nowIST  = new Date(Date.now() + IST_OFFSET_MS)\nconst dateStr = nowIST.toLocaleDateString('en-IN', {\n  day: 'numeric', month: 'short', year: 'numeric'\n})\n\nconst loginMap = {}\nfor (const log of todayLogs) {\n  if (log.clock_in) loginMap[log.user_id] = log.clock_in\n}\n\nconst lateThisMonth = {}\nfor (const log of monthLogs) {\n  if (!log.clock_in) continue\n  const dt   = new Date(log.clock_in)\n  const hhmm = `${String(dt.getHours()).padStart(2,'0')}:${String(dt.getMinutes()).padStart(2,'0')}`\n  if (hhmm > LATE_CUTOFF) {\n    lateThisMonth[log.user_id] = (lateThisMonth[log.user_id] || 0) + 1\n  }\n}\n\nconst members = users.filter(u => u.role !== 'ADMIN')\nconst onTime  = []\nconst late    = []\nconst absent  = []\n\nfor (const m of members) {\n  const clockInRaw = loginMap[m.id]\n  const lateCount  = lateThisMonth[m.id] || 0\n  const passesLeft = Math.max(0, MAX_PASSES - lateCount)\n\n  if (!clockInRaw) {\n    absent.push({ name: m.name, employee_id: m.employee_id, passesLeft })\n  } else {\n    const dt          = new Date(clockInRaw)\n    const hhmm        = `${String(dt.getHours()).padStart(2,'0')}:${String(dt.getMinutes()).padStart(2,'0')}`\n    const timeDisplay = dt.toLocaleTimeString('en-IN', {\n      hour: '2-digit', minute: '2-digit', hour12: true, timeZone: 'Asia/Kolkata'\n    })\n    if (hhmm <= LATE_CUTOFF) {\n      onTime.push({ name: m.name, time: timeDisplay })\n    } else {\n      late.push({ name: m.name, time: timeDisplay, lateCount, passesLeft })\n    }\n  }\n}\n\nfunction pad(str, len) {\n  return str.length >= len ? str : str + ' '.repeat(len - str.length)\n}\n\nlet msg = `\\uD83D\\uDCCA Login Report — ${dateStr}, 10:00 AM\\n\\n`\n\nif (onTime.length > 0) {\n  msg += `\\u2705 On Time (${onTime.length}):\\n`\n  for (const m of onTime) msg += `• ${pad(m.name, 18)}— ${m.time}\\n`\n  msg += '\\n'\n}\n\nif (late.length > 0) {\n  msg += `\\uD83D\\uDD50 Late — pass used (${late.length}):\\n`\n  for (const m of late) {\n    const warn = m.passesLeft === 0 ? ' ALL USED' : ''\n    msg += `• ${pad(m.name, 18)}— ${m.time}  (${m.lateCount}/${MAX_PASSES} used${warn})\\n`\n  }\n  msg += '\\n'\n}\n\nif (absent.length > 0) {\n  msg += `\\u274C Not Logged In (${absent.length}):\\n`\n  for (const m of absent) msg += `• ${pad(m.name, 18)}${m.passesLeft} passes left\\n`\n  msg += '\\n'\n}\n\nif (!onTime.length && !late.length && !absent.length) msg += 'No active members found.\\n'\n\nmsg += '— GroFast System'\n\nreturn [{ json: { adminPhone, message: msg } }]"
      },
      "id": "wf2-n06",
      "name": "Build Admin Report",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1340, 300]
    },
    {
      "parameters": {
        "resource": "message",
        "operation": "send",
        "recipientPhoneNumber": "={{ $json.adminPhone }}",
        "messageType": "text",
        "textBody": "={{ $json.message }}"
      },
      "credentials": {
        "whatsAppApi": { "id": "1", "name": "WhatsApp account" }
      },
      "id": "wf2-n07",
      "name": "WhatsApp Admin",
      "type": "n8n-nodes-base.whatsApp",
      "typeVersion": 1,
      "position": [1560, 300]
    }
  ],
  "connections": {
    "Schedule Trigger": { "main": [[{ "node": "Config", "type": "main", "index": 0 }]] },
    "Config": { "main": [[{ "node": "Get Users", "type": "main", "index": 0 }]] },
    "Get Users": { "main": [[{ "node": "Get Today Logs", "type": "main", "index": 0 }]] },
    "Get Today Logs": { "main": [[{ "node": "Get Month Logs", "type": "main", "index": 0 }]] },
    "Get Month Logs": { "main": [[{ "node": "Build Admin Report", "type": "main", "index": 0 }]] },
    "Build Admin Report": { "main": [[{ "node": "WhatsApp Admin", "type": "main", "index": 0 }]] }
  },
  "active": false,
  "settings": { "executionOrder": "v1" },
  "tags": []
}
